defaults:
  - default@_here_ # デフォルトの値を設定してから上書きする

# ver"5"の5foldを使用
epochs: 800 #100
t_max: 900
scheduler_name: "cosine"
model_name: "resnet50d"
seed: 5555
n_folds: 5
use_ema: False
ema_decay: 0.997
ema_warmup_steps: 500 #500
patience: 40
