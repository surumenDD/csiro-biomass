defaults:
  - default@_here_ # デフォルトの値を設定してから上書きする

# ver"2"の5foldを使用
epochs: 500 #100
t_max: 800
scheduler_name: "pleateau"
model_name: efficientnet_b2
seed: 42
n_folds: 5
use_ema: True
ema_decay: 0.95
ema_warmup_steps: 1000 #500
patience: 20
